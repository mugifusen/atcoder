#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for (int i = 0; i < (n); ++i)

// A問題で時間を溶かした結果やってません...
int main(void) {
    int n;
    vector<unsigned int> a(n);
    rep(i,n) cin >> a[i];
    unsigned int sum=0;
    for (unsigned x = 0;; ++x) {
        for (int i = 0; i < n; ++i) {
        }
    }
}