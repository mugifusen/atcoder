#include <bits/stdc++.h>
using namespace std;

int main(void) {
    // 途中なので後で続きする(1/27 22:51)
    int n,q_n;
    cin >> n >> q_n;
    vector<pair<int, char>> q(q_n);
    for (auto& qi : q) {
        cin >> qi.first >> qi.second;
    }
    
}