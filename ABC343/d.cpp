#include <bits/stdc++.h>
using namespace std;

int main(void) {
    int n, t;
    cin >> n >> t;
    vector<int> a(t), b(t), player(n,0);
    for (int i = 0; i < t; ++i) {
        cin >> a[i] >> b[i];
    }
    map<int, int> mp;
    for (int i = 0; i < t; ++i) { 
        mp
    }
    return 0;
}