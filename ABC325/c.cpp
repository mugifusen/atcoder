#include <bits/stdc++.h>
using namespace std;

int main(void) {
    int h,w;
    cin >> h >> w;
    vector<string> s(h);
    for (int i = 0; i < h; ++i) {
        cin >> s[i];
    }
    for (int i = 0; i < h; ++i) {
        for (int j = 0; j < w; ++j) {
            
        }
    }
}
